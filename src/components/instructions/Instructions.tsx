import React from "react";

export const Instructions = () => {
  return (
    <div id="instructions">
      You will be assigned either synonyms or antonyms. Selecting the wrong type
      gives a point to the other player. The inviter goes first. When a player
      matches a correct pair, they choose the next tile; if not, the turn
      passes. A single distractor tile is included—once only the distractor
      remains, the game ends, and the player with the most correct pairs wins.
    </div>
  );
};
